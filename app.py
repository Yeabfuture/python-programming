# We can either import the whole module by using the import statement or
import modules

# Or we can specifcally import the direct finctions we need for that specific file.
#When we call those specific methods or functions wecan yse them directly in the code but if we are importing the whole module we have to attach it with the class name
from modules import lbs_to_kg
print(lbs_to_kg(70))

